name: Build

on:
  pull_request:
  push:
    branches:
      - '**'
      - '!trunk'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v5

    - name: Build
      run: docker build .
